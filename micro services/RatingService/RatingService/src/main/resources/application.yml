server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/mircoservices_r
    username: postgres
    password: Anurath@123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  application:
    name: RATINGSERVICE

eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8084/eureka
